name: Deploy workflow
on: push
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: lint
        run: echo "lint..."
      - name: test
        run: echo "test..."
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: deploy
        run: echo "deploy..."
